{
  "name": "hub-integracao",
  "version": "0.1.0",
  "description": "Hub de Integração: sincronização de imóveis do WordPress/Listivo para a OLX PRO",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "build": "tsc --noEmit",
    "dev": "vercel dev"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@vercel/node": "^3.2.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
